PROCEDURE <<PROCEDURE_NAME>>(
 )
    LANGUAGE SQLSCRIPT
    SQL SECURITY INVOKER
    AS
BEGIN
  TRUNCATE TABLE "<<TARGET_TABLE>>";
  INSERT INTO "<<TARGET_TABLE>>" SELECT "ROW_INDEX", "MODEL_CONTENT", 1, CURRENT_UTCTIMESTAMP FROM "<<SOURCE_VIRTUAL_TABLE>>";
  SELECT * FROM "<<TARGET_TABLE>>";

END